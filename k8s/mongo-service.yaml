apiVersion: v1 
kind: Service 
metadata:
    name: mongo
    labels:
      app: mongo
      tier: database
spec:
  selector:
    app: mongo
    tier: database
  clusterIP: None
  ports:
   - port: 27017
     targetPort: 27017